<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= locals.title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link rel="stylesheet" href="https://fontawesome.com/search#icongrid">
    <style> :root {
            --primary-green: #00a67d;
            --light-text: #f3f4f6;
            --secondary-color: #008767;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: #f7f7f7; 
        }
        .event-card {
            transition: transform 0.3s ease;
        }
        .event-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.08);
        }
        .detail-icon {
            color: var(--secondary-color);
        }
        input:focus {
            border-color: var(--primary-green) !important;
            box-shadow: 0 0 0 1px var(--primary-green) !important;
        }</style>
</head>
<body class="min-h-screen antialiased">
    <nav class="bg-[var(--primary-green)] shadow-md">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8 h-16 flex justify-between items-center">
            <div class="flex items-center text-white text-xl font-bold">
                <i data-lucide="calendar-check" class="w-6 h-6 mr-2 text-white"></i>
                <span class="text-white">EMS</span>
                <span class="ml-2 font-light text-sm hidden sm:inline">Event Management System</span>
            </div>
            <div class="flex items-center space-x-4">
                <button class="text-white hover:text-gray-200 p-1 rounded-full transition duration-200">
                 <a href = "/addevent">Add Event</a>
                </button>
                <button class="text-white hover:text-gray-200 p-1 rounded-full transition duration-200">
                 <a href = "/logout">Logout</a>
                </button>
                <button class="text-white hover:text-gray-200 p-1 rounded-full transition duration-200">
                 <a href = "/signup">Add Admin</a>
                </button>
                <button class="text-white hover:text-gray-200 p-1 rounded-full transition duration-200">
                    <i data-lucide="user-circle" class="w-7 h-7"></i>
                </button>
            </div>
        </div>
    </nav>
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-6 border-b pb-2">Manage Events</h1>
        <p class="text-gray-600 mb-8">View, edit, or delete events.</p>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
            <% for(let i of Event_List){ %>
            <div class="event-card bg-white p-5 rounded-xl shadow-lg border border-gray-100">
                <h2 class="text-xl font-bold text-gray-900 mb-3"><%= i.name%></h2>
                
                <div class="space-y-3 text-sm">
                    <div class="flex items-center text-gray-700">
                        <i data-lucide="calendar" class="w-4 h-4 detail-icon mr-2"></i>
                        <span><%= i.date.toLocaleDateString("en-IN")%></span>
                    </div>
                    <div class="flex items-center text-gray-700">
                        <i data-lucide="clock" class="w-4 h-4 detail-icon mr-2"></i>
                        <span><%= i.time%></span>
                    </div>
                    <div class="flex items-center text-gray-700">
                        <i data-lucide="map-pin" class="w-4 h-4 detail-icon mr-2"></i>
                        <span><%= i.venue%></span>
                    </div>
                    <div class = "length of participants">
                        <i class="fa-solid fa-users"></i>
                        <span><%= i.participants.length%></span>
                     </div>
                   </div>
                <div class="flex gap-3 mt-4 pt-3 border-t border-gray-100">
                    
                    <button class="flex-1 text-sm bg-green-600 text-white py-2 rounded-lg font-medium hover:bg-green-700 transition duration-150">
                       <a href = "/viewdetails/?id=<%=i._id%>">View Details</a>
                    </button>
                
                    <a href = "/delete-contact/?id=<%=i._id%>">
                    <button class="flex-1 text-sm bg-red-600 text-white py-2 rounded-lg font-medium hover:bg-red-700 transition duration-150">
                       <a href = "/delete-contact/?id=<%=i._id%>">Delete</a>
                    </button>
                   
                </div>
            </div>
             <% } %>
        </div>
        <!-- <div class="text-center pt-8 border-t border-gray-200">
            <button type="button" class="bg-green-600 text-white py-3 px-8 rounded-lg font-semibold hover:bg-green-700 focus:outline-none focus:ring-4 focus:ring-green-500/50 transition duration-150 text-lg shadow-lg flex items-center mx-auto">
                <i data-lucide="plus-circle" class="w-5 h-5 mr-2"></i>
               <a href = "/addevent">Add New Event</a>
            </button>
        </div>-->
        <script>
            lucide.createIcons();
        </script>
    </div>
</body>
</html>
